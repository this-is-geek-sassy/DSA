#include <bits/stdc++.h>

using namespace std;
int main() {

    string s = "striver";
    // cout << s << endl;

    // cout << strlen(s.c_str()) << endl;
    // cout << s.length() << endl;

    for (size_t i = 0; i <= s.length()+1; i++)
    {
        cout << s[i] << " ";
    }
    cout << endl;
}